# Optional Implementer Brief

Use only for a task whose delegation is justified under SKILL.md. Invoke tools according to current runtime schemas; no fixed model or effort fields are prescribed here.

Provide: focused objective; relevant source/contract pointers; scope and file ownership; baseline; constraints; acceptance; expected evidence.

Implement only the assigned work. Preserve unrelated changes. Prefer test-first for regressions/core behavior; use proportionate verification and reuse still-valid results. Do not run a full suite merely because a commit is next. Commit only if authorized and at a meaningful boundary.

Self-review against the requirement and report changed files, actual checks/results, limitations, and material questions. Do not spawn another reviewer or start a fix loop on your own. A concise return is sufficient unless durable evidence was explicitly requested.
